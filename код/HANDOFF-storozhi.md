# HANDOFF — сторожи + вход + панель (сеанс 23-24.07)

Продолжение работы в новом чате/терминале. Читай ЭТО + память в
`/Users/qq/.claude/projects/-Users-qq-untitled-folder/memory/` (MEMORY.md — индекс).

## ЗАДЕПЛОЕНО и работает (ветка railway-engines, коммиты cfce608/bx.., деплой web+ig-worker)
- **СТОРОЖИ** — авто-слежка за креаторами. Движок `storozhi.cjs` (пул-чтение облачными
  работягами → baseline медианой → спайк ×2.5 → бренд на новый пост + мобилизация 6 работяг).
  В супервизоре `engines.cjs` (name=storozhi, off=STOROZHI_OFF). Гейт `radar_config.storozhi_enabled`
  (сейчас ВКЛ). Тик каждые 10 мин, per-креатор каденс в SQL. Панель: блок «⚙️ Сторожи» в Радаре
  (тумблеры/крутилки/светофор + кнопка «Обновить прокси» → `genproxies.cjs`). API: `/storozhi/config`,
  `/storozhi/proxies`.
- **ФИКС ВХОДА** (radar.ts passwordLogin): после ввода 2FA-кода ждём окно подтверждения до ~60с +
  спиннер-гейт + onetap/Save-info = успех (раньше закрывал на 24с → «не дождался окна»). + maybeSessions
  не метит свежий-никогда-не-входивший акк как dead.
- **Баг daily_limit** починен: колонкой владеет ТОЛЬКО `/storozhi/config`; авто-раздача выпилена из UI.
- Группы: **300-ые** (карантин, role=blocked), **ЗАПАСНЫЕ** (резерв, role=reserve — лежат, берутся когда
  боевой пул проседает). Роли реально фильтруются в выборках (worker/blocked/reserve).
- Движки-инструменты (в репо): `runtask.cjs` (авто-замена акка до добивания), `brandblast_par.cjs`
  (параллельный бренд-бласт), `grabgrid_cloud.cjs` (грид креатора через облако), `genproxies.cjs`,
  `capdetect.cjs`, `shadowcheck.cjs` (шэдоу, CHP-гейт локально).

## ХВОСТЫ (не сделано — по приоритету)
1. **Видимость логина на карточках** (боль владельца): «в очереди / логинится / почему не зашёл
   (soft_block/2FA-отбит/need_login)». Сейчас на карточке только session_status.
2. **~16 ЗАПАСНЫХ ещё не залогинены** — фикс входа помогает (3 ожили), но day-0 soft-block часть
   отбивает; им нужен 3ч кулдаун → ретрай/recycle. НЕ ручной ввод кодов (сиды валидны, проблема была тайминг).
3. **Живость сторожей в панели** (последний тик, спайки, кого забрендили).
4. **UX-экономия места**: карточка акка под `<details>` «⚙ настройки»; оценка 1-10 → `<select>`;
   простыня дежурного (index.html:483) под details; зелёная плашка «Посты» приглушить; мёртвый JS
   ридера ~130 строк (readerExtract/saveReader/... index.html:1772+) вычистить.
5. **Физический снос бэкенда** хэштег-радара (maybeRadar/maybeAutoReply/reader-эндпоинты) — сейчас
   ДОРМАНТ (нет ридера=no-op, auto_reply=false), функционально выпилено, осталась чистка кода.
6. **Счётчик бренда** в статус-строке панели (top-level бренд падает в «ответы»).
7. **shadowTick на Railway** (авто-проверка тени) — в дизайне, не вшит.
8. **Софт реабилитации акков** (ава/ник/сторис) — отложено (расходник).
9. **Авто-промоушен ЗАПАСНЫХ** в бой при проседании пула.

## ПРАВИЛА (критично, из памяти)
- НИКОГДА pkill Orbita/gologin (сносит все окна). Свои node — по имени скрипта.
- Локальные анон-тулы (observer/latestpost/shadowcheck) — только CHP=1 (не с ноута владельца).
- Деплой: `railway up` тащит рабочее дерево → stash чужого (vcomment/backlog_safe/duty_safe) перед
  деплоем, pop после. Коммить только свои файлы. tsc-green обязателен.
- ТГ-уведы выключены (токен удалён на web+ig-worker, откат в scratchpad/tg_restore.txt).
- Без длинных тире в текстах; обращение «начальник».

## КАК ПРОДОЛЖИТЬ ВХОД ЗАПАСНЫХ
Дать воркеру дожевать (maybeRelogin, 10/проход, кулдаун 1ч) — фикс входа теперь ждёт окно.
НЕ логинить руками параллельно (двойная сессия). Смотреть панель: карточки → «сессия жива».
