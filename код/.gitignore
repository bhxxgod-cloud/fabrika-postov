node_modules/
.env
*.log
dist/
.DS_Store

# бэкапы редакторов/чатов
*.bak
*.bak2

# модели компьютерного зрения (yunet, sface): лежат локально, в гит не едут
*.onnx

# Тяжёлые исходники и отбраковка — в гит не идут (ревизия 14.08, 3.4 ГБ)
src_unfit/
src_pending/
src_pending2/
src_bad/
